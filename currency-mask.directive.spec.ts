import { CurrencyMaskDirective } from './currency-mask.directive';

describe('CurrencyMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyMaskDirective();
    expect(directive).toBeTruthy();
  });
});
